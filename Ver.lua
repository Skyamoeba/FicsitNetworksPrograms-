local json = { _version = "0.1.2" }

-------------------------------------------------------------------------------
-- Encode
-------------------------------------------------------------------------------

local encode

function Ver.Version()
  update = 100
  return (update)
  end

function json.encode(val)
  return ( encode(val) )
end


return json
